<style>
    html,body {
        margin: 0;
        padding: 0;
    }

    html {
        height: 100%;
    }

    body {
        font-family: Inter,-apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Oxygen,Ubuntu,Cantarell,Fira Sans,Droid Sans,Helvetica Neue,sans-serif;
        background-color: #269;
        background-image: linear-gradient(white 1.5px, transparent 1.5px),
        linear-gradient(90deg, white 1.5px, transparent 1.5px),
        linear-gradient(rgba(255, 255, 255, .3) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255, 255, 255, .3) 1px, transparent 1px);
        background-size: 100px 100px, 100px 100px, 20px 20px, 20px 20px;
        background-position: -2px -2px, -2px -2px, -1px -1px, -1px -1px;
    }

    h1, h2, h3, h4, h5, h6 {
        margin: 0 0 20px 0;
    }

    h2:after {
        left: 0;
        display: block;
        width: 100%;
        height: 2px;
        margin-top: 2px;
        content: " ";
        background: #000;
    }

    table {
        border-collapse: collapse;
        width: 100%;
    }

    td {
        line-height: 25px;
        vertical-align: top;
        border-top: 1px solid gray;
    }

    input[type='submit'] {
        background-color: #269;
        color: white;

        height: 35px;
        width: 100%;
        margin-top: 20px;

        border: 1px solid black;
        border-radius: 25px;

        transition: 0.3s;
        cursor: pointer;
    }

    input[type='submit']:hover {
        background-color: #4e84ad;
    }

    footer {
        text-align: center;

        line-height: 30px;
        margin: 10px 0 10px 0;
        background: rgba(203, 213, 224, 0.9);
    }

    a, a:active, a:hover, a:visited {
        color: black;
    }

    #container {
        width: 95%;
        margin: 0 auto;
    }

    #logo {
        text-align: center;
        margin: 10px 0 20px 0;
    }

    #share-info {
        height: 150px;
    }

    #share-link {
        background: rgba(203, 213, 224, 0.9);
        border-radius: 15px;
        margin: 0 10px 20px 10px;
        padding: 20px;
    }

    .column {
        background: rgba(203, 213, 224, 0.9);

        border-radius: 15px;

        margin: 0 10px 20px 10px;
        padding: 20px;

        overflow-wrap: break-word;
    }

    .table-item {
        padding-right: 130px;
    }

    @media screen and (min-width: 900px) {
        .column {
            display: flex;
            flex-direction: column;
            flex-basis: 100%;
            flex: 1;

            width: 50%;
        }

        .row {
            display: flex;
            flex-direction: row;
            flex-wrap: wrap;
            width: 100%;
        }
    }

    @media screen and (max-width: 400px) {
        #container {
            margin: 0;
            width: 100%;
        }

        #share-link {
            margin: 0 0 20px 0;
            border-radius: 0;
        }

        .column {
            margin: 0 0 20px 0;
            border-radius: 0;
        }
    }
</style>
