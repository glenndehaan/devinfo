<style>
    html,body {
        margin: 0;
        padding: 0;
        height: max-content;
    }

    body {
        font-family: Inter,-apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Oxygen,Ubuntu,Cantarell,Fira Sans,Droid Sans,Helvetica Neue,sans-serif;
        background-image: linear-gradient(170deg, rgba(255, 103, 0, 0.45), rgba(255, 103, 0, 0)), linear-gradient(#11377c, #11215b);
        background-size: cover;
        background-repeat: no-repeat;
    }

    h1, h2, h3, h4, h5, h6 {
        margin: 0 0 20px 0;
    }

    h2:after {
        left: 0;
        display: block;
        width: 100%;
        height: 2px;
        margin-top: 2px;
        content: " ";
        background: #000;
    }

    table {
        border-collapse: collapse;
        width: 100%;
    }

    td {
        line-height: 25px;
        vertical-align: top;
        border-top: 1px solid gray;
    }

    input[type='submit'] {
        background-color: #269;
        color: white;

        height: 35px;
        width: 100%;
        margin-top: 20px;

        border: 1px solid black;
        border-radius: 25px;

        transition: 0.3s;
        cursor: pointer;
    }

    input[type='submit']:hover {
        background-color: #4e84ad;
    }

    footer {
        text-align: center;

        line-height: 30px;
        margin: 10px 0 10px 0;
        background: rgba(203, 213, 224, 0.9);
    }

    a, a:active, a:hover, a:visited {
        color: black;
    }

    #container {
        width: 95%;
        margin: 0 auto;
    }

    #logo {
        text-align: center;
        margin: 10px 0 20px 0;
    }

    #share-info {
        height: 150px;
    }

    #share-link {
        background: rgba(203, 213, 224, 0.9);
        border-radius: 15px;
        margin: 0 10px 20px 10px;
        padding: 20px;
    }

    .column {
        background: rgba(203, 213, 224, 0.9);

        border-radius: 15px;

        margin: 0 10px 20px 10px;
        padding: 20px;

        overflow-wrap: break-word;
    }

    .table-item {
        padding-right: 130px;
    }

    @media screen and (min-width: 900px) {
        .column {
            display: flex;
            flex-direction: column;
            flex-basis: 100%;
            flex: 1;

            width: 50%;
        }

        .row {
            display: flex;
            flex-direction: row;
            flex-wrap: wrap;
            width: 100%;
        }
    }

    @media screen and (max-width: 400px) {
        #container {
            margin: 0;
            width: 100%;
        }

        #share-link {
            margin: 0 0 20px 0;
            border-radius: 0;
        }

        .column {
            margin: 0 0 20px 0;
            border-radius: 0;
        }
    }
</style>
